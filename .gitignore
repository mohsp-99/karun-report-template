# Python
venv/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Build artifacts — every generated document goes into a build/ folder.
# The folders themselves are kept (via .gitkeep); their contents are ignored.
# Checked-in sample outputs under samples/ are NOT affected.
**/build/*
!**/build/.gitkeep
